背景及现有条件：
1、现有三维点云大小是3200*3200*Z，
2、传感器会提供同等带下的二维图像，
3、我会对二维图像进行特征筛选，只筛选去有效区域，并给出有效区域的XY值，这部分由我来做
需要创建的算法：
4、你创建的这个算法接口要对接我提供的XY坐标数组，然后用来从原始点云中提取相应的子点云
5、使用提取的子点云使用最小二乘法进行平面拟合，，可以使用numpy高效计算库
6、然后将拟合的平面方程输出
最后处理：
7、同时显示在界面UI的点云显示控件上
界面功能优化：
8、将点云设备管理界面的点云显示控件和模板编辑中的点云显示控件合并，如果两个功能是一样的
那么保留模板编辑中的接口，
9、缩小模板编辑界面中的分割栏左侧区域的产品模板列表显示区域，
10、将模板编辑界面中的分割栏右侧区域中的detection config，other config,model config三个区域做成弹窗方式，
添加参数弹窗按钮，用来弹出参数配置窗口，然后进行配置参数，该参数需要对应当前模板的功能记得保留，只是修改UI的显示方式
